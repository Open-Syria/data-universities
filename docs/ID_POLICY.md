# ID Policy

University IDs must:

- start with `sy-`,
- use lowercase ASCII,
- use hyphen-separated words,
- remain stable once released.

Pattern:

```text
sy-{institution-slug}
```

Example:

```text
sy-damascus-university
```
